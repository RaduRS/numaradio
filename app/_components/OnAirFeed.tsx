"use client";
// POLLING DISABLED — DB is down, no live data.

export function OnAirFeed() {
  return (
    <div style={{ color: "var(--fg-mute)", fontSize: 13, padding: "18px 0" }}>
      Nothing on the air log yet — static presentation mode.
    </div>
  );
}